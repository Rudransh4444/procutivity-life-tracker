# Local AI Productivity — MVP

Scaffold for a local-first productivity app. Platform: Electron + React.

Run:

1. npm install
2. npm run start

This repo was scaffolded by the Copilot CLI. See plan.md in the Copilot session state for the implementation plan.
