{
  "name": "local-ai-productivity",
  "version": "0.1.0",
  "description": "Local-first AI productivity MVP",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node src/groq/adapter.test.js",
    "vercel-build": "vite build",
    "quartz:prepare": "node scripts/quartz_prepare.js",
    "quartz:build": "npx quartz build --source quartz_vault --out public/quartz || echo \"Quartz CLI not installed; run 'npm i -g @quartz/cli' or use 'npx quartz'\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.20.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-icons": "^5.6.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "vite": "^6.1.0"
  }
}
